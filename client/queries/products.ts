// import { gql } from '@apollo/client';

// export const GET_PRODUCTS_BY_LIMIT_AND_OFFSET = gql`
//     query getAll {
//         getProducts {
//             amount
//             currency
//             imageUrl
//             name
//         }
       
//     }
// `;

/**
 * query getProductsByLimitAndOffset($limit: SearchLimit, $offset: SearchOffset) {
        getProductsByLimitAndOffset(limit: $limit, offset: $offset) {
            amount
            currency
            id
            imageUrl
            name
        }
    }
 */